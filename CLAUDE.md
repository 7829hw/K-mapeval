# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The guidance itself lives in @AGENT.md — commands, layering rules, the invariants that break
silently, GeoFlow operator wiring, concurrency and cache behavior, and dataset conventions. It is
the single source of truth shared with other coding agents; make edits there, and keep this file a
pointer.
